[package]
name = "echoit"
version = "0.1.1"
edition = "2024"
description = "Echoes back the argument you pass it."
license = "MIT"
repository = "https://github.com/merikan/echoit"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }

[dev-dependencies]
assert_cmd = "2.2.2"
predicates = "3.1.4"
